import { AppContent } from '@/components/app-content';

const Home = () => {
    return (
        <AppContent variant="header">
            <div>asd</div>
        </AppContent>
    );
};

export default Home;
